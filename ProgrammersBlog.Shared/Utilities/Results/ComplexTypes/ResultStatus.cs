namespace ProgrammersBlog.Shared.Utilities.Results.ComplexTypes
{
    public enum ResultStatus
    {
        // Enum => ? 
        Succes = 0,
        Error = 1,
        Warning = 2,
        Info = 3
    }
}
